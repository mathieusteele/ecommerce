<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title><%= title %></title>
    <link
      rel="stylesheet"
      type="text/css"
      href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css"
    />
  </head>

  <body>
    <%- include ("../includes/nav.ejs") %>
    <div class="jumbotron">
      <div class="container">
        <h1><%= title %></h1>

        <% if (errors.length > 0) { %> Errors (<%= errors.length %>):

        <ul>
          <% for (let error of errors) { %>
          <li>
            <%= error %>

            <form method="POST" action="/admin/clear-error">
              <input
                id="book"
                type="hidden"
                name="error"
                value="<%= error %>"
              />
              <button type="submit">OK</button>
            </form>
          </li>
          <% } %>
        </ul>

        <% } %>

        <h2>Are you sure you want to delete this product?</h2>

        Title: <%= productTitle %><br />
        Author: <%= productAuthor %>

        <form method="POST" action="/admin/remove-product">
          <input type="hidden" name="oldTitle" value="<%= productTitle %>" />
          <input type="hidden" name="oldAuthor" value="<%= productAuthor %>" />

          <button type="submit">Really Delete</button>
          <a href="/admin">Cancel</a>
        </form>
      </div>
    </div>
    <div class="container"></div>
  </body>
</html>
